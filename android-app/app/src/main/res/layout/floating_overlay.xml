<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- Main Draggable Button -->
    <ImageButton
        android:id="@+id/floating_main_btn"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="#CC6C63FF"
        android:src="@android:drawable/ic_menu_camera"
        android:elevation="8dp"
        android:layout_gravity="top|end"/>

    <!-- Expanded Control Panel -->
    <LinearLayout
        android:id="@+id/control_panel"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#EE1A1A2E"
        android:padding="8dp"
        android:visibility="gone"
        android:layout_marginTop="64dp"
        android:elevation="8dp">

        <Button
            android:id="@+id/btn_ai_analyze"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:text="📸 AI Analyze"
            android:backgroundTint="#6C63FF"
            android:textSize="12sp"
            android:layout_marginBottom="4dp"/>

        <Button
            android:id="@+id/btn_open_capcut"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:text="🎬 Open CapCut"
            android:backgroundTint="#FF6B35"
            android:textSize="12sp"
            android:layout_marginBottom="4dp"/>

        <Button
            android:id="@+id/btn_tap_center"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:text="👆 Tap Center"
            android:backgroundTint="#333355"
            android:textSize="12sp"
            android:layout_marginBottom="4dp"/>

        <Button
            android:id="@+id/btn_stop"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:text="🛑 STOP"
            android:backgroundTint="#FF3333"
            android:textSize="12sp"/>

    </LinearLayout>
</FrameLayout>
